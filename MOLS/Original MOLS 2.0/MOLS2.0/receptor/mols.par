	parameter (maxatm = 500, maxpar = 60, maxord = 60, 
     $		   maxhb = 200, maxstr = 3000,  maxres = 50 ,maxi=1000,
     &             mnatp = 9000)

c	common /par/ natom, ntor, nhb, ns, lres
c	common /atm/ atmno(maxatm),atname(maxatm),rename(maxatm),resno(maxatm)
c	common /var/ ivat(maxres*8,4),vty(maxres*8),ivres(maxres*8)
c	character atname*4,rename*4

